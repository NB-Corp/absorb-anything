{
  "name": "absorb-anything",
  "version": "0.1.0",
  "description": "CLI for absorbing external material into evidence, analysis, and reusable knowledge.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "bin": { "absorb": "./dist/cli.js" },
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^12.1.0",
    "commander": "^12.1.0",
    "absorb-anything-core": "workspace:*"
  }
}
